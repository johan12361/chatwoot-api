import { getConversationFilter } from '../apis/conversations/getConversationFilter.js'
import { addConversation } from '../apis/conversations/addConversation.js'

export const conversations = {
  getConversationFilter,
  addConversation
}
