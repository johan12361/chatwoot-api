import { getContactFilter } from '../apis/contacts/getContactFilter.js'
import { addContact } from '../apis/contacts/addContact.js'
import { getContactById } from '../apis/contacts/getContactById.js'

export const contacts = {
  getContactFilter,
  addContact,
  getContactById
}
